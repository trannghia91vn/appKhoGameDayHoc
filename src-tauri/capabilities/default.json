{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal permissions for the launcher UI window.",
  "windows": ["main"],
  "permissions": ["core:default", "deep-link:default", "dialog:default"]
}

